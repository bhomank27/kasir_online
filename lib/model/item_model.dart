class Item {
  Item({
    required this.id,
    required this.name,
    required this.total,
    required this.price,
    required this.totalPrice,
    required this.isSelected,
  });

  int id;
  String name;
  int total;
  double price;
  int totalPrice;
  bool isSelected;
}
